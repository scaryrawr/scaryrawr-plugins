# ScaryRawr Plugins

Learning and sharing how to write claude code plugins.

## Installing

```sh
claude plugin marketplace add scaryrawr/scaryrawr-plugins
```

### Installing a Plugin

Either use `/plugin` or from the terminal:

```sh
# Installs the typescript native lsp plugin
claude plugin install typescript-native-lsp
```
